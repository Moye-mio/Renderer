---
description: C++ 类/结构体成员变量命名规范（统一使用 m_lowerCamelCase 前缀）
globs: ["**/*.h", "**/*.hpp", "**/*.cpp", "**/*.cc", "**/*.cxx"]
alwaysApply: true
---

# C++ 成员变量命名规范

## 核心规则

类（`class`）与结构体（`struct`）的成员变量一律使用 **`m_` 前缀 + 小写开头的驼峰命名**，即 `m_lowerCamelCase`。

- 正确：`m_viewMatrix`、`m_position`、`m_commandList`、`m_currentFrameIndex`、`m_bufferRegistry`
- 错误：`mViewMatrix`、`mPosition`、`mCommandList`（`m` 后直接跟大写，缺少下划线）
- 错误：`view_matrix`、`viewMatrix`、`m_ViewMatrix`（缺少前缀 / 首字母大写）

```cpp
class Camera
{
private:
    dvec3 m_position;
    mat4  m_viewMatrix = mat4(1.0);
    double m_moveSpeed = 5.0;
};
```

## 缩写（Acronym）处理

当成员名以缩写开头时，缩写整体转为小写，避免出现 `m_vS`、`m_tLAS` 这类难看写法：

| 原名 | 规范名 |
|---|---|
| `mVS` | `m_vs` |
| `mFS` | `m_fs` |
| `mTLAS` | `m_tlas` |
| `mTLASInstanceCount` | `m_tlasInstanceCount` |

缩写位于名字中间时，保持其原有大小写即可：`mCurrentVAO` → `m_currentVAO`（`VAO` 不变）。

## 适用与排除范围

- **适用**：本项目自有代码中所有 `class` / `struct` 的成员变量声明。
- **不适用（禁止改动）**：
  - 第三方库的字段名。典型如 Assimp 的 `aiMesh::mVertices`、`aiScene::mRootNode`、`aiFace::mIndices`、`aiScene::mMaterials`、`mNumMeshes`、`mNumVertices` 等——这些是外部 API 字段，只是恰好以 `m` 开头，**不得重命名**。
  - 局部变量、函数参数、临时变量（不加 `m_` 前缀，使用普通 `lowerCamelCase`）。
  - 无前缀约定的 POD/聚合结构体的公有数据字段若项目已有其它统一约定，遵循该文件的既有约定。

## 重命名注意事项

- 成员变量在其类的 `.cpp` 实现文件中通常有大量引用，重命名时必须**同步修改声明与所有使用处**，否则编译失败。
- 使用**词边界**匹配替换，避免误伤：如把 `mBuffer` 改名时不能影响 `mBuffers`、`mBufferRegistry`。
- 同名成员可能分布在多个类/文件（如 `mDevice`、`mStream`），确认它们都应统一为同一规范名后再批量处理。
- 改名后建议按模块分批编译验证（先 RendererCore，再 RendererGL / RendererVK），确保每步编译通过。
